include: package:flutter_lints/flutter.yaml

analyzer:
  # language:
  #   strict-raw-types: true
  #   strict-casts: true
  plugins:
    - custom_lint
  exclude:
    - "**/*.g.dart"
    - "**/*.gr.dart"
    - "**/*.freezed.dart"
    - "**/*.gen.dart"

linter:
  rules:
